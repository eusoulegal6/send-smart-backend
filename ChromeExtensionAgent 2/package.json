{
  "name": "chrome-extension-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal Chrome extension scaffold with a dist build output.",
  "scripts": {
    "build": "node scripts/build.mjs"
  }
}
